# Which frontend address is allowed to call this API.
# Change this if your dev server runs on a different port.
CORS_ORIGINS=http://localhost:5173

# Name of the SQLite database file.
# It is created automatically inside backend/data/ on first run.
DB_FILENAME=analyzer.db
