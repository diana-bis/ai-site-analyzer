# Base URL of the backend API. Only variables prefixed with VITE_ are
# exposed to client-side code (Vite convention) — this keeps non-VITE_
# secrets from ever being bundled into the browser JS.
VITE_API_BASE_URL=http://localhost:8000
